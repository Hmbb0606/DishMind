### 面向烹饪场景的中文 RAG 智能问答系统  
**AI 应用开发**  
2025.10 - 2026.01  

项目简介：面向日常烹饪场景的 RAG 智能问答系统，以 200+ 道本地 Markdown 菜谱为知识库，围绕离线索引构建、在线混合检索、跨语言桥接与前端体验深化完成全链路工程优化，支持菜谱推荐、步骤讲解、食材查询、追问上下文理解、菜谱收藏与交互式分步做菜，实现从检索到跟做的闭环体验。

技术栈：Python、LangChain、FAISS、BM25、BAAI/bge-small-zh-v1.5、OpenAI-compatible SDK、React 19、TypeScript、Vite、Tailwind CSS、Express、Docker  

#### 技术亮点：
- **离线索引优化**：基于 Markdown 标题层级对 200+ 道菜谱进行结构化分块，自动绑定分类、菜名、难度等元数据；构建 FAISS 稠密向量与 BM25 稀疏索引双通路，并通过内容指纹校验实现启动时自动判定增量重建或秒级加载，消除知识库变更后的手动运维成本。
- **在线检索优化**：设计查询意图路由，将用户问题分为列表推荐、分步讲解、一般问答三类并分发不同 Prompt 策略；引入菜名直匹配优先通道，query 命中 dish_name 后直接绕过嵌入检索，针对性解决长尾菜名召回率低的问题；向量 + BM25 双路召回后通过倒数排名融合（RRF）重排，辅以元数据过滤与追问上下文拼接提升多轮问答连贯性。
- **前端体验设计**：基于 React 19 + TypeScript 构建 SPA 界面，实现纯规则 Markdown 解析管线，将 LLM 输出的菜谱文本无损转换为结构化数据，统一驱动收藏持久化、格式化复制、详情弹窗与交互式分步做菜四个功能模块；分步烹饪模式下自动从步骤文本中提取时间描述生成倒计时，用户可按步骤逐条跟进并标记完成。
