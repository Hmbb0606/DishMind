# 可选：前端服务端口，默认 3000
PORT=3000

# 本地开发默认使用 conda 模式；Docker 部署默认使用 direct 模式。
PYTHON_BRIDGE_MODE=conda
CONDA_ENV_NAME=cook-rag
PYTHON_BIN=python

# Python RAG 使用的模型密钥请配置在项目根目录 `.env` 或 Docker env 文件中。
# 例如：
# DEEPSEEK_API_KEY=your_key
# OPENAI_API_KEY=your_key
# MOONSHOT_API_KEY=your_key
